terraform {
  backend "s3" {
    bucket       = "terraform-state-fca43277"
    key          = "eks-cluster/terraform.tfstate"
    region       = "us-east-1"
    use_lockfile = true
    encrypt      = true
  }
}
